# Example env file (NO SECRETS)

# Git identity (not a secret)
GIT_AUTHOR_NAME=
GIT_AUTHOR_EMAIL=www.stason4ik@gmail.com

# Messenger (examples)
MESSENGER_JWT_SECRET=
POSTGRES_PASSWORD=
MINIO_ROOT_PASSWORD=

# SMS provider (if used)
SMS_PROVIDER_API_KEY=
